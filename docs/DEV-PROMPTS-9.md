# 超级赞 Super-Z · 待开发功能提示词库(第九辑:体验硬化)

> 背景:第八辑收口后做了一轮前端体验审计(P0/P1/P2 已落地,见 git log 的
> 「前端体验审计」三个提交)。审计过程中挖出的、不属于展示层的缺口在这一辑排。
>
> 通用约定见 `docs/DEV-PROMPTS.md` 顶部(平台口径:外卖 5% 封顶 /
> 团购核销 2% / 住宿 5% 离店计佣 / 配送费 100% 归骑手;金额分存整数;
> 中文报错;完成后不 push)。

## 本辑约束(默认遵守)

- **不依赖平台补贴**。任何方案都不能建立在"平台出钱买增长"上——
  这是平台的根本立场,不是预算问题。
- **不新增用户可感知的费率**。5% 是上限不是目标。
- 涉及资金的改动必须过 `services/audit.py` 的恒等式与公开账本
  (改完跑 `e2e_reversal_audit` + `e2e_p4_witness`)。

---

### 114. 骑手新单推送(抢单模式的动力来源)

```
给超级赞补上骑手新单推送。先读 docs/DEV-PROMPTS-9.md 顶部约束与 docs/DEV-PROMPTS.md 通用约定。

现状(2026-07-29 审计发现):server/app/services/push.py 只有四个推送场景——notify_order_status(推用户)、notify_new_order(推商家老板)、notify_review_reply(推用户)、notify_favorites(推收藏用户)。**没有任何"有新单可抢"的推送给骑手**;骑手唯一的新单信号是骑手端每 5 秒一次的 availableOrders 轮询(rider_app/lib/main.dart 的 _refresh,发现 fresh 单才响铃 + 振动)。后果:App 一退后台骑手就是盲区。auto_flow.py 的 _notify_no_rider 确实会推在线骑手,但那是"已经超时没人接"的兜底告警,不是正常新单;等它触发时单子已经凉了。
因为这个缺口,前端只能把后台轮询降频到 20 秒而不能停(见 rider_app 的 _startPolling 注释),电量还在白烧。
业务规则(已拍板):
- 触发点:订单进入待接单池时推一次。即支付成功(services/payment_core.py 的 mark_order_paid,那里已经在推商家了)且该单确实需要骑手——**到店自取(pickup)与商家自配送(self_delivery)的单不推**;
- 收件人:is_online 为真的骑手,且订单商家在该骑手的接单半径内(users.grab_radius_km,null=不限;骑手最后上报位置取现有的位置上报数据)。半径外的不推——推了也抢不到,只会训练骑手忽略推送;
- **合并与限频**:同一骑手 60 秒内最多收一条。窗口内又来新单就合并成「附近有 N 个新单」,不要一单一条;用 Redis 记窗口(参照 auto_flow 里 maybe_run_daily_audit 的防重手法);
- 文案:单条给「商家名 → 收货区域,配送费 ¥X」(配送费是骑手最关心的);合并条给「附近有 N 个新单,最高 ¥X」。**不写"快抢""手慢无"这类话术**;
- 点击进抢单大厅并刷新;单子已被别人抢走时提示「这单已被接走,看看其他的」,不弹错误;
- 骑手下线(is_online=false)后不再收到;这是骑手自己的开关,不做"引诱上线"的推送。
技术要点:推送失败绝不阻塞下单主流程(照现有 push 的写法只记日志);push_logs 表记一条便于排查"没收到提醒";合并逻辑放 services/push.py 内部,调用方只管"这单进池了";注意 mark_order_paid 是在事务里调的,推送要放到 commit 之后发(参照 _notify_no_rider 的写法)。
前端配套(rider_app):
- 推送到位后把后台轮询彻底停掉(现在是降频 20 秒的妥协),前台保持 5 秒兜底;
- PushService 的通知点击路由到抢单大厅。
验收:e2e_rider_push.py——造一单支付成功,断言在线且半径内的骑手收到一条(用 push_logs 断言,不真发);半径外的骑手不收;60 秒内第二单合并不新增条数;自取单与自配送单不推;下线骑手不推。回归 e2e_orders + e2e_reversal_audit。
```

---

## 竞品分析结论(2026-07-29)

功能清单已经比多数同类完整(拼单/预约/自取/追加/超时赔付/发票/售后/申诉/
实名/团购核销)。空间不在"再加功能",在三处:

1. **有实现与公开立场相冲突**——邀请有礼、生日券、复购提醒的券都是平台承担
   (`referrals.py` 头注释写着 subsidy 口径,`marketing.py` 三个任务同理),
   而用户端「我们承诺不做的事」印着"不靠补贴换增长"。金额不大,方向是错的。
2. **零补贴下唯一能规模化的增长引擎是"让商家带客"**,而商家端一件推广物料
   都没有(无店铺码、无海报、无分享、无专属链接)。商家在这里每单多赚十几个点,
   他们有动力,平台没给工具。
3. **真实风险不是功能缺,是供给密度**。低单量 → 骑手赚不到 → 送得慢 →
   用户流失 → 单量更低。解药(顺路聚合/自配送/自取)三样都已经做出来了,
   但都不是主线。

### 115. 营销发券的资金方归位:平台不再出钱

```
把邀请有礼/生日券/复购提醒三处的发券资金方从平台改成商家。先读 docs/DEV-PROMPTS-9.md 顶部约束。

现状:server/app/routers/referrals.py 头注释明写「券为平台承担(subsidy 口径)」,reward_referral_if_first_order 直接 db.add(Coupon(...)) 且不带 funder(默认 platform);services/marketing.py 的 run_birthday / run_winback / run_favorites 走 admin 建的 trigger=birthday/winback 批次,同样是平台预算。而用户端「钱去哪了」页的「我们承诺不做的事」里印着「不靠补贴换增长,也就不会有断补后的涨价」——产品行为和公开承诺不一致,被扒出来是硬伤。
基建已经齐了:Coupon 有 funder(platform/merchant)与 merchant_id 字段,CouponBatch 支持 merchant_id + total 预算封顶 + per_user_limit,商家自建券接口 POST /merchants/me/coupon-batches 已存在(trigger="shop",成本商家承担、平台按券后实收计佣)。
业务规则(已拍板):
- CouponBatch.trigger 增加 "referral"(商家自建的新客推荐券)。商家在店铺设置里可开可关、自定面额与预算总量,与现有店铺券同一套 UI 和同一套预算封顶;
- reward_referral_if_first_order 改为:看被邀请人首单落在哪家商家 → 该商家有启用中的 referral 批次就从批次发两张(邀请人 + 被邀请人各一张,限该店可用、funder=merchant);**没有批次就不发券**,只把邀请关系记成 rewarded,并推「你的好友已经在超级赞下单了」——不再由平台兜底出钱;
- run_birthday / run_winback / run_favorites 同理:只从 merchant_id 非空的批次发;平台批次(merchant_id 为空)一律不再新建,已存在的存量批次跑完即止,admin 建批次接口移除 birthday/winback 的平台选项;
- **不改任何已发出的券**,存量券照常可用——改的是以后不再发平台券;
- 用户端邀请页文案同步:从"你俩各得券"改成"你的好友首单那家店会送你俩券(参与店铺)";没有参与店铺时不许暗示有券。
技术要点:funder=merchant 的券下单时走 discount 口径(与满减同通道),不进 subsidy——审计恒等式与公开账本因此都不用改,但改完必须跑 e2e_reversal_audit + e2e_p4_witness 确认;coupons.source 唯一约束继续防重发;referral 批次的每人限领沿用 per_user_limit。
验收:e2e_referral_funding.py——商家没建 referral 批次时完成首单只记 rewarded 不发券;建了批次则双发且 funder=merchant、merchant_id 正确、只在该店可用;批次 total 发完自动停;存量平台券不受影响。回归 e2e_orders + e2e_reversal_audit + e2e_p4_witness。
```

### 116. 商家专属码与一键海报(零补贴增长引擎)

```
给商家端补推广物料:店铺专属码 + 一键生成海报。先读 docs/DEV-PROMPTS-9.md 顶部约束。

现状:商家端(apps/merchant_app)没有任何推广物料——搜遍全端没有店铺二维码、没有海报、没有分享入口、没有专属链接。商家在超级赞每单多赚十几个点(被抽 5% 而不是 20%),他们有动力把老客带过来,但只能靠嘴说。而这恰恰是零补贴模式下唯一能规模化的获客渠道:平台不出钱,商家自愿推。
用户端已有 share_card.dart(晒「钱去哪了」分账图,离屏渲染成图片再分享),海报可照它的做法。
业务规则(已拍板):
- 服务端加一个稳定的店铺短链 /s/{merchant_id}(或短码),打开后:装了 App 直接进店铺页,没装则落到一个轻量落地页(server/static)介绍这家店 + 下载入口。落地页照现有 legal-*.html 的做法做静态页,不引外部依赖;
- 商家端「店铺」Tab 加「推广物料」入口,一键生成海报图并调系统分享:店名 + 店铺码 + 一句「在超级赞下单,这家店只被抽 5%,省下的在菜价里」+ 平台 Logo。海报离屏渲染(照 share_card 的写法),不需要服务端出图;
- 海报文案**只讲事实不讲话术**:不许出现"最低价""立减""限时"这类词;费率数字取该店真实 commission_rate,不写死 5%(阶梯佣金下可能是 4.5%);
- 商家如果建了店铺券或 referral 批次(#115),海报上多印一行「扫码领本店 X 元券」;没建就不印,不做空头承诺。
技术要点:短链不要暴露内部 id 规律,用 merchants 表加一个 short_code(6 位,建店时懒生成、唯一);海报尺寸按微信分享习惯出 3:4;字体用已打包的 SzSerif/SzSans,不额外加资源;分享走 share_plus(商家端已有依赖)。
验收:e2e_shop_shortcode.py——短码唯一、可解析回店铺、无效码 404;商家端实机生成海报并成功唤起系统分享;海报在深色模式下仍是浅底(离屏渲染固定浅色,照 share_card 的处理)。
```

### 117. 老客召回交给商家(平台只给工具不给钱)

```
把复购召回从"平台掏钱推"改成"商家自己决定"。先读 docs/DEV-PROMPTS-9.md 顶部约束,依赖 #115 的资金方归位。

现状:services/marketing.py 的 run_winback 是平台任务——平台挑出 30 天没下单的用户、用平台批次发券、平台推送。商家看不到自己的沉睡客,也决定不了发不发、发多少。
业务规则(已拍板):
- 商家端对账页/店铺页加「老客召回」:显示「这 N 位客人 30 天没来了」(只给数量与匿名分布,**不给手机号、不给姓名**——那是用户隐私,平台不能转手给商家);
- 商家可对这批人发一次召回券(用 #115 的商家批次机制,成本自负、预算封顶),平台负责触达;
- 频控沿用现有克制原则:每人每自然周 ≤2 条营销推送、用户可在「我的」一键关闭、同一商家对同一用户每月最多召回一次;
- 平台侧的 run_winback 改为只跑商家批次,平台不再自建。
技术要点:沉睡客的口径与现有 run_winback 一致(30 天前有完成单、近 30 天无单),但要按 merchant_id 过滤成"这家店的老客";触达仍走 push_to_user 并记 push_logs;绝不向商家返回可定位到个人的字段。
验收:e2e_merchant_winback.py——商家只看到数量不看到个人信息;发券走商家批次且预算封顶生效;关闭营销推送的用户不被触达;同一商家同一用户一个月内不重复。
```

### 118. 首页补筛选器(接口已支持,前端没接)

```
把搜索页已有的筛选能力搬到首页。先读 docs/DEV-PROMPTS-8.md 的「设计基线」。

现状:server/app/routers/merchants.py 的 search_merchants 已经支持 max_distance_m / min_rating / has_promo / max_min_order_cents 四个筛选,但首页列表接口(nearby)只有 sort 与 category。用户在首页想找"起送价低的""有满减的"找不到,只能先去搜索页——而搜索页要先输入关键词才有结果。
业务规则(已拍板):
- nearby 接口补齐与 search 同名同语义的四个筛选参数(同名同义很重要,否则两处口径会漂);
- 首页排序 chips 那一行右侧加「筛选」入口,点开底部弹层:距离、评分、有优惠、起送价四项,选中项数在入口上显示为角标;
- **筛选不改变排序口径**——「没有竞价排名,排序只按你选的口径」这句承诺不能因为加筛选而变味,筛选只做过滤;
- 筛选后无结果时给 SzEmpty +「放宽筛选」按钮,不给空白页。
技术要点:参数校验与 search 复用同一套(距离 100–20000、评分 0–5、起送价 0–1000 元);筛选状态不持久化(下次进首页回到默认),避免用户忘了自己开着筛选却以为附近没店。
验收:四个筛选各自生效且可组合;清空后回到默认列表;无结果时按钮能一键清空;e2e_merchants_filter.py 覆盖参数边界与非法值 422。
```

### 119. 「我的常点」:复购从"再来一单"进化到"直接加购"

```
把复购路径从"重进店铺再选一遍"缩短到"一键加购"。先读 docs/DEV-PROMPTS-8.md 的「设计基线」。

现状:用户端首页的「再来一单」是最近点过的 6 家**店铺**(按商家去重),点进去还要重新选菜。行业数据是重度用户的订单高度集中在三四个固定组合上,让他们重建购物车是纯粹的摩擦。
业务规则(已拍板):
- 新增「我的常点」:按(商家 + 菜品组合)统计近 90 天下单次数,取前 5 个组合;
- 一键把整个组合加进购物车并直达结算页(缺货/下架/改价的菜自动剔除并提示「X 已下架,已从这单去掉」,不静默改单);
- 位置在首页「再来一单」之上;没有满 2 次的组合时整块不显示(新用户不看到空模块);
- 组合命名用菜品名拼接(「红烧牛肉面 + 卤蛋」),不自动起名。
技术要点:统计放服务端(新接口 GET /orders/frequent),别在客户端拉全量订单算——那正是我们刚修掉的分页问题;口径只算 completed 单;价格以当前价为准并明确提示与上次的差额,不用历史价下单。
验收:e2e_frequent_orders.py——次数统计正确、下架菜被剔除并有提示、改价有提示、不足 2 次不返回;首页模块在无数据时不占位。
```

### 120. 堂食扫码入口(策略级,需先拍板)

```
说明:这条会把产品边界从"外卖平台"扩到"到店 + 外卖",要不要走这条路是老板决策,不是技术选型。先确认再开工。

思路:商家已有的堂食客流是免费且精准的流量池。桌贴二维码 → 扫码看本店菜单 / 领本店券 / 收藏店铺,把线下客流转成 App 用户。Toast 与 Square 就是靠这个从 POS 长成平台的。团购券到店核销已经跑通,说明到店场景在你这里是通的。
需要先定的:扫码后是"只看菜单和领券"(轻,几天),还是"堂食下单 + 桌号 + 出餐"(重,要动订单模型加桌台概念、要考虑堂食不收配送费的口径、要考虑后厨打印)。建议先做轻的验证转化,别一上来做重的。
```

### 121. 低密度城市把自取与自配送做成主推(策略级,需先拍板)

```
说明:这条是运营策略在产品上的体现,不是新功能——三样解药(顺路聚合 sameShop/sameWay、商家自配送 self_delivery、到店自取 pickup)都已经做出来了,只是都不在主线上。

问题:零补贴 + 低抽成,商家愿意来(省钱),但骑手为什么来?配送费 100% 归骑手听着好,一天只有 5 单的话收入还是低。冷启动会走成:骑手少 → 送得慢 → 用户流失 → 单量更少 → 骑手更少。
思路:在单量密度不够的城市/时段,首页默认主推「附近可自取,省 3 元配送费」,而不是把外卖配送当唯一形态。用户省钱、商家不用等骑手、平台不用先凑出骑手密度。等单量起来再切回配送优先。
开工前先看数据:现有订单里 pickup 占比多少。如果很低(大概率),说明不是用户不要,是入口太深——自取现在藏在结算页的一个开关里。
```

---

## 执行记录(#114 / #116–#119)

2026-07-30 一轮做完并全量回归通过(`make test` 378 项绿)。与提示词的出入都在这里说明,
以实际落地的口径为准。

### #116 商家专属码与一键海报 — 已完成

- `merchants.short_code`(6 位,字母表去掉易混的 I/O/0/1),迁移 `0065_merchant_short_code`;
  懒生成:第一次调 `GET /merchants/me/promo` 才建号,不给可能永远不用的店占号段。
- `GET /merchants/me/promo` 返回短码 / 短链 / 店名 / **本店真实费率** / 在发的店铺券面额。
  费率取 `shop.commission_rate` 而不是写死 5% —— 阶梯佣金降到 4.5% 的店,海报要跟着变。
- `GET /merchants/by-code/{code}` 公开反查(大小写不敏感),`GET /s/{code}` 落地页
  (`server/static/shop.html`,与 App 同一套色板令牌,深浅色自适应)。
  短码不存在也回 200 页面说人话,不甩裸 404 给扫码的人。
- 商家端 `promo_page.dart`:海报客户端离屏渲染(RepaintBoundary→PNG,3x),
  固定取浅色令牌(截图要能打印),share_plus 分享 / 复制短链。
- 新增 `public_base_url` 配置,**默认就指生产域名** —— 海报是印出来贴墙上的,
  漏配一次就是一批废二维码。
- e2e:`e2e_merchant_promo.py`(短码幂等、费率一致、反查、404、越权)。

### #118 首页筛选器 — 已完成(与提示词有出入)

- `GET /merchants` 加 `min_rating` / `has_promo` / `max_min_order_cents`,与 `/merchants/search`
  同一套口径与校验;距离上限直接复用已有的 `radius_m`,**没有另开 `max_distance_m`**
  (同一个语义两个参数名是给自己挖坑)。
- 无定位兜底路径同样认筛选 —— 否则用户一关定位,筛选就静默失效。
- 筛选与排序分开呈现:排序改次序,筛选改集合;选中项数写在入口标签上。
- 筛完为空时给「清空筛选」出口,与"该品类还没商家"的招商位区分开(两件事,出口不同)。
- 未做:提示词里的 `e2e_merchants_filter.py` 改名为 `e2e_home_filters.py`,与 #119 #117 合并一条。

### #119 我的常点 — 已完成(口径按单品,不按组合)

- `GET /orders/frequent`:近 90 天 completed 单里按「店 + 单品」聚合取前 5。
- **与提示词的出入**:提示词写的是「菜品组合」整组加购。实际做成**单品**复购。
  理由:组合口径下改一个菜就命中不了任何历史组合,越是老用户越容易一条都出不来;
  单品口径下"想点哪个点哪个",反而更接近"把复购摩擦降到最低"的原意。
  整单重下的需求由已有的「再来一单」承接,两块并存、语义不重叠。
- 只列还在售且有库存的菜 —— 列出来点不了比不列更糟。店打烊了照常列出但淡显,
  藏起来用户会以为自己的常点丢了。
- 价格取当前价(接口直接返回 `price_cents`),不用历史价下单。

### #117 老客召回交给商家 — 已完成

- `GET /merchants/me/winback`:只给 30 天 / 90 天沉睡人数与半年老客总数,**不给名单**。
  商家看不到是谁、手机号、更导不出来 —— 名单一旦交出去就再也收不回来。
  e2e 里有一条专门断言响应体里不含任何顾客身份信息。
- 顺带修掉一个真 bug:`run_winback` / `run_birthday` 原先用 `_active_batch(trigger)`
  随便挑**一家店**的批次,然后发给**全平台**符合条件的人 —— 那家店在替所有同行买单。
  改成按店切开(`_active_batches`),各发各的老客,预算各自封顶;
  沉睡口径也从"全平台没下过单"收敛到"没在本店下过单"。
- 商家端 `winback_page.dart`:看人数、发一批券、随时停;文案明说券钱商家出、平台不出补贴。

### #114 骑手新单推送 — 已完成

- `notify_riders_new_grab`:商家接单(单子进抢单池)那一刻推给附近**在线**骑手,
  按骑手自己设的抢单半径过滤。仍是广播抢单,不改成强制派单 —— 只是把广播送到耳边。
- 只在 ACCEPTED 推一次(READY 时单子早在池里了,再推是骚扰);
  自取 / 商家自送 / 追加单不进池,自然也不推;每单每人一次(Redis nx)。
- 取不到骑手位置就不推:宁可漏推,也不把 20 公里外的单推到人脸上。
- `record_skip=True` 留痕:骑手是最可能事后追问"我怎么没收到这单"的一方,
  push_logs 让这件事可查而不是各执一词。

### 回归中修掉的既有缺陷(与本轮功能无关,但会误导排查)

1. **接单超时计时基准错了**(真 bug):`商家 5 分钟未接单自动取消` 用 `Order.updated_at` 计时,
   而 `updated_at` 有 `onupdate=now()` —— 风控异步回写 `risk_flags`、隐私号绑定、ETA 重估
   任何一次行写入都会把它顶回当前时间。等于订单每被碰一次就重新给商家 5 分钟,
   用户那边则是一直没人接也不自动退款。改用 `coalesce(rider_pool_since, updated_at)`,
   `rider_pool_since` 只在支付成功/骑手转单时刷新。
2. **两条住宿账本用例用了本机日期**:账本按北京时区切日,开发机在 PDT 时本地日期落后北京一天,
   昨日锚点里一行住宿都找不到(KeyError/StopIteration),且只在一天中的某几个小时复现。
3. **`orderable_dish` 会挑到酒类**:买酒要先实名,普通用例的账号没实名,
   撞上就是一个跟本用例毫无关系的 422。顺带补上在售与库存过滤。
4. **`e2e_p3_touch` 用「最近 50 条推送日志」数条数**:演示账号的窗口早被历史触达撑满,
   新增一条就挤掉一条老的,计数恒等永远不成立。改成按本次运行的唯一内容断言。
5. **`e2e_auth_sms` 声称幂等但不是**:`sms:day:p:{phone}` 按自然日累加,
   一天跑第二遍第 1 条就要滑块。用例开头只清自己那个测试号的计数。

### 本地跑全量 e2e 的前置条件(踩过的坑)

- 服务端必须 `AUTO_FLOW_ENABLED=false` 启动:后台清扫会和用例自己调的 `sweep_once` 抢同一批订单。
- 全量跑会超过「同 IP 每日 20 条验证码」的生产限流(用例要注册几十个新号)。
  本地跑时循环清 `sms:day:ip:*` 即可,**不要清 `sms:day:p:*`** —— `e2e_auth_sms` 靠它触发滑块。
- 演示库的菜会被历次跑动抽干,库存见底后一批用例连环失败;跑前把低库存菜补回去。
