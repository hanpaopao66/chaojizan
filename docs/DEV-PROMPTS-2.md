# 超级赞 Super-Z · 待开发功能提示词库(第二辑)

> 用法同第一辑(docs/DEV-PROMPTS.md):「通用约定」+ 想做的那条一起贴给 AI。
> 编号接续第一辑(1-24 已全部交付),本辑 25-44。
> 标注 ⚖️ 的条目含未拍板的产品决策,开工前先跟发起人确认。
>
> **进度:25-44 已全部交付 ✅(2026-07)。**⚖️ 条目按文中"建议拍板"默认执行:
> #33 非免责转单 5 次当日限抢(不罚款次日恢复);#34 ETA+超时发 3 元安抚券走
> subsidy 口径(最小 coupons 表);#36 自配送配送费归商家(并入商家入账行,
> 佣金只抽餐费);#37 文本本地敏感词同步拦截+图片先发后审;#40 拼单发起人
> 一次性支付(Redis 共享购物车,平台不碰代收分账)。如需调整,对着提示词改。

## 通用约定

沿用 docs/DEV-PROMPTS.md 的通用约定(技术栈/铁律/迁移/测试/费率口径/不 push)。
补充两条:
- 涉及骑手的新规则,必须与产品立场自洽:**抢单制,不强制派单、不罚款**;
  考核类功能一律"统计公示 + 软约束",不做扣钱。
- 新增资金字段(小费/赔付)必须过 services/audit.py 恒等式与 witness 校验,
  改口径要同步改 witness/superz_witness.py 并跑 e2e_p4_witness。

## 已有能力对照(别重复开发)

| 需求 | 现状 |
|---|---|
| 实名认证 | 骑手:RiderProfile 身份证+审核;用户:#14 二要素桩+加密落库 |
| 健康证上传 | RiderProfile.health_cert_photo_url,审核必查 |
| 商家资质审核 | 食品经营许可证必传+管理后台人工审核(M4) |
| 用户投诉工单 | routers/tickets.py 客服工单;食安投诉走 #4 红线通道 |
| 隐私号 | #13 AXB 桩+全链路脱敏(打码/小票/privacy_phone) |
| 到店自取 | 完整上线:免配送费、4位取餐码核销、超时自动完成、e2e_pickup |
| 骑手转单 | #9 自助转单回池+每日免责计数;"改派"见 26 |
| 恶劣天气加价 | platform_flags weather_surcharge,加价全归骑手;"停运"见 35 |
| 抢单排序 | #10 综合分(距离+等待加权)+顺路标记;"半径过滤"见 28 |

---

## 运力与骑手线

### 25. 用户小费(含无人接单加小费)

```
在 super-z 仓库开发「用户小费:100% 归骑手」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:配送费 100% 归骑手已是铁律;无人接单只有 10 分钟提醒/30 分钟取消兜底(auto_flow),用户没有主动加价促接单的手段。
业务规则(建议拍板):
- 下单时可选小费(0/2/5/10 元档+自定义,上限 50);支付后无人接单时(提醒线触发)用户端提示"加小费让骑手更快接单",可追加(追加走独立支付,mock 通道直接成功,微信通道未接前降级为不允许追加只允许下单时给);
- 小费 100% 归骑手,平台分文不取、不计佣金基数;骑手抢单池的订单卡显眼展示小费;
- 取消订单小费随单全退;小费不参与满减/首单立减计算。
技术要点:orders.tip_cents(迁移);create_order 收 tip_cents 入 total;结算时骑手行 amount = delivery_fee + tip;审计"配送侧 Σ配送费=Σ骑手入账"口径改为 Σ(配送费+小费),witness 校验器同步(payload 加 tip 字段或并入 delivery 口径,历史锚点不受影响);抢单池 OrderOut 已有 distance_m,加 tip_cents 展示;三端 UI:结算页小费选择、订单详情"小费"行、骑手端订单卡小费徽标。
验收:e2e_tip.py——带小费下单骑手结算多得小费;取消全退(含小费)流水一致;佣金基数不含小费;审计+witness 回归绿。
```

### 26. 管理员改派

```
在 super-z 仓库开发「配送改派(管理员)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:骑手可自助转单(#9);管理员看到卡单(骑手失联但没转单)时没有处置手段,只能等配送异常仲裁。
业务规则(建议拍板):
- 管理后台订单/配送异常面板可对 ACCEPTED/READY 的单执行:①释放回池(等同强制转单,原骑手不计免责次数,写事件留痕) ②指定改派给某在线骑手(下拉选在线骑手,对方收推送);
- PICKED_UP 的单不可改派(餐在人手上),走配送异常仲裁;
- 原骑手收推送告知(客气文案:订单已由平台协调改派);用户商家不推送。
技术要点:POST /admin/orders/{no}/reassign {rider_id 可空=回池};复用 #9 的释放逻辑(rider_pool_since 刷新/追加单跟随/事件 to_status='reassigned' note 记操作人);指定改派校验目标骑手在线+已认证+在途上限;admin.html 配送异常面板加按钮。
验收:e2e_reassign.py——回池后他人可抢;指定改派 rider_id 落库+超上限 409;PICKED_UP 409;原骑手转单免责计数不受影响。
```

### 27. 运力看板(骑手热力图 + 调度总览)

```
在 super-z 仓库开发「管理后台运力看板」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:骑手位置在 Redis(RIDER_LOC_KEY,5 分钟过期);管理后台有数据看板但无运力视角;订单坐标齐全。
业务规则(建议拍板):
- 管理后台新面板「运力」:地图上叠三层——在线骑手位置(绿点)、待抢订单(橙点,含等待时长)、在途订单商家→收货连线;30 秒自动刷新;
- 指标卡:在线骑手数/待抢单数/在途单数/近1小时人均单/超10分钟无人接单数(红);
- 订单密度热力:近 2 小时下单坐标聚合成网格热力(前端渲染,不引重型库);
- 只看不派:看板是眼睛,处置动作走 26 改派。
技术要点:GET /admin/dispatch-overview(聚合 Redis 位置+DB 订单,一次返回);admin.html 用天地图浏览器端 Key(TIANDITU_BROWSER_KEY 服务端下发给管理页)或无 key 时画网格示意(参照 delivery_map 降级思路);热力=前端把点按 ~300m 网格聚合画半透明色块;位置数据脱敏原则:仅管理员可见,不进公开接口。
验收:e2e_dispatch_board.py——接口返回骑手/订单聚合结构正确;无 Redis 位置不报错;非 admin 403。
```

### 28. 骑手接单半径

```
在 super-z 仓库开发「骑手自设接单半径」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:抢单池返回 200 条内综合分前 50(#10),不按骑手位置过滤——骑手会看到全城的单;users 表无骑手偏好字段。
业务规则(建议拍板):
- 骑手可设接单半径:1/2/3/5/不限(默认不限,km);池子只返回商家距我 ≤ 半径的单;无定位时忽略半径(全量,按等待排序);
- 半径过滤在综合分排序之前;顺路单(same_shop/same_way)豁免半径限制(手头单顺路的永远给看);
- 骑手端抢单大厅顶部半径切换 chip,选择持久化。
技术要点:users.grab_radius_km(迁移,nullable=不限)或 RiderProfile 上加;available-orders 在 Python 层按 haversine 过滤(位置已取);ApiClient+骑手端 chip;设置接口 PATCH /riders/me/preferences。
验收:e2e_grab_radius.py——设 2km 后远单不出现、顺路单豁免;无定位全量;改回不限恢复。
```

### 29. 骑手在线时长与考勤

```
在 super-z 仓库开发「骑手在线时长统计(只统计不考核)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:is_online 开关+位置心跳(15 秒保活)在 Redis;上下线不留历史,无时长统计。
业务规则(建议拍板):
- 记录每次上线/下线(位置心跳超 5 分钟断档视为掉线,清扫任务补写下线);
- 骑手端「我的数据」:今日/本周在线时长、完成单数、每在线小时收入(自我参考);
- 管理后台骑手面板加近 7 天在线时长列;不做全勤奖惩,数据供将来运力规划。
技术要点:rider_sessions 表(rider_id, online_at, offline_at nullable,迁移);/riders/online 开关时写表;auto_flow 清扫段扫 Redis 位置过期但 session 未关的补 offline_at;聚合接口 GET /riders/me/worklog;admin rider-profiles 聚合列。
验收:e2e_rider_worklog.py——上下线写记录;心跳断档被清扫补下线(backdate);时长聚合口径正确。
```

### 30. 骑手上岗管理(培训考试 + 装备申领)

```
在 super-z 仓库开发「骑手培训考试与装备申领」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:骑手实名+健康证审核通过即可上线;无培训环节;无装备管理。
业务规则(建议拍板):
- 上岗培训:内置题库(交通安全/食安卫生/平台规则,20 题选 10,80 分过;题目存 JSON 不建后台编辑器,改题改代码);实名审核通过后、首次上线前必须通过一次;已在跑的存量骑手 7 天宽限期(过渡开关);
- 装备申领:头盔/餐箱/雨衣登记制(平台成本价或免费,拍板)——骑手端申领,管理后台标记发放,留领用记录;不强制佩戴检查(拍照打卡之类的监控不做,信任+抽查);
- 培训通过与装备状态在管理后台骑手面板可见。
技术要点:rider_exams(rider_id, score, passed, answers JSONB, 时间)+rider_gear(rider_id, item, status requested/issued, note)两表一个迁移;题库 server/app/data/rider_quiz.json;set_online 校验 passed(宽限开关 config);骑手端考试页(逐题+交卷评分)+装备页;admin 面板两列+发放按钮。
验收:e2e_rider_onboarding.py——未通过考试上线 403(宽限关);80 分过、59 不过可重考;申领→发放留痕。
```

### 31. 骑手意外险

```
在 super-z 仓库开发「骑手意外险(对接桩)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:每笔配送入账已计提骑手保障金(services/ledger.py,从平台佣金拨出,公开账本可见)——钱有了,但没有"险"。
业务规则(建议拍板):
- 照支付桩模式对接保险服务商(众安/泰康在线的按天骑手意外险,config 留 insurance_* 字段):未配置=登记模式(记录应投保天数,保障金池兜底,骑手端明示"平台保障金先行赔付");配置后=骑手每日首次上线自动投当日单(费用从保障金池出);
- 保单记录骑手端可查(哪天投了/保单号/保额说明);理赔入口指引(联动 32 事故流程);
- 账目透明:保障金池「计提-支出」在公开账本已有计提行,支出行(投保费/赔付)同样入账本。
技术要点:rider_insurance_days(rider_id, day, policy_no, premium_cents, status)迁移;services/insurance.py 桩(ensure_today(rider) 未配置只落记录);set_online 钩子挂投保(失败不阻塞上线,清扫补投);保障金池支出的账本行设计要过 witness(新增 payload 字段按当日口径,历史锚点不动);骑手端保障页。
验收:e2e_rider_insurance.py——上线落当日记录且幂等;未配置状态标 registered;账本回归绿。
```

### 32. 交通事故处理流程

```
在 super-z 仓库开发「骑手交通事故上报与处置」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:配送异常(delivery_issues)覆盖订单维度的异常;人身事故没有独立通道,只能打客服电话。
业务规则(建议拍板):
- 骑手端显眼入口「事故上报」(不限于配送中):一键页先展示急救/报警指引(120/122 大按钮),再上报——地点(自动取定位)/伤情程度/现场照片/是否涉及第三方;
- 上报即三动作:①当前在途订单全部自动无责转单回池(复用 #9,不计次数) ②管理后台红色加急工单 ③平台电话回访(后台标记跟进状态 open/following/closed,处置留痕 JSONB 参照食安);
- 与 31 联动:事故日已投保则展示保单号与理赔指引;未投保走保障金先行赔付(线下,后台留痕);
- 处理 SOP 写进 docs/RIDER_SOP.md(客服照着打电话)。
技术要点:rider_accidents 表(迁移);POST /riders/accidents(伤情 severity 枚举,照片可后补——人先安全);上报时批量释放在途单(事务);admin 面板(参照食安面板样式,红色加急);推送管理员(record_skip)。
验收:e2e_rider_accident.py——上报后在途单全部回池且不计转单次数;工单落库;补传照片;后台留痕闭环。
```

### 33. ⚖️ 转单考核公示(替代"拒单惩罚")

```
在 super-z 仓库开发「转单考核规则公示与软约束」。先读 docs/DEV-PROMPTS-2.md 通用约定。

⚖️ 待拍板:发起人提的是"拒单惩罚",但抢单制下没有派单就没有"拒单",且平台立场是不罚款。
本条按立场改写为:转单(抢了又退)的考核公示+软约束。同意这个改写再开工。

现状:#9 转单每日免责 2 次,超出计数(管理后台近 30 天列,>10 标红);等餐超时无责;没有任何约束动作和规则公示。
业务规则(建议拍板):
- 软约束:同一自然日非免责转单达 5 次(config 可调),当日 23:59 前暂停抢单(不罚钱、不封号,次日自动恢复);到店等餐超时/事故转单永不计入;
- 规则公示页:骑手端「规则中心」把转单/考核/结算/申诉规则全文列出(静态内容+当前计数),做到"规则先说清"(对齐官网入驻页话术);
- 触达:第 3/4 次转单时推送提醒"再转 N 次今日将暂停抢单"。
技术要点:grab 接口校验当日非免责转单数(Redis 已有计数,拆免责/非免责两个键——#9 的键语义要升级,免责的不 incr 已实现);骑手端规则页(shared 静态文案+接口取计数);config transfer_daily_suspend_threshold。
验收:e2e_transfer_discipline.py——第 5 次非免责转单后抢单 409(文案含次日恢复);免责转单不计;次日(backdate Redis 键)恢复。
```

### 34. ⚖️ 订单超时赔付(准时宝-lite)

```
在 super-z 仓库开发「送达超时自动安抚赔付」。先读 docs/DEV-PROMPTS-2.md 通用约定。

⚖️ 待拍板:赔付成本谁承担。按平台立场建议:平台承担、不扣骑手不扣商家
(出餐超时已有商家维度统计,骑手不罚款是立场)。金额口径建议配送费的 50% 以券形式补。
需要先做平台通用券(第一辑 M5 挂起项)或直接退现金——退现金动账本,券不动。拍板后开工。

现状:预计送达时间没有产品化(无 ETA 字段);超时无赔付;催单/致歉已有。
业务规则(建议草案):
- 下单时生成预计送达时间(距离/出餐时长的朴素公式,显示"预计 XX:XX 前送达",预约单=预约时间);
- 实际送达超过预计 15 分钟:自动给用户发安抚券(无门槛 3 元,7 天有效)+致歉推送;每单最多一次;
- 超时归因只统计不追责(商家出餐慢/骑手配送慢/无人接单,后台可见,供改进);
- 极端天气(35 停运开关开启前后 1 小时)与用户改地址过的单不赔。
技术要点:orders.eta_at(迁移,支付时算);送达 transition 判超时;券系统若未建,先做最小 coupons 表(user_id, amount_cents, min_spend, expires_at, used_order_id)+下单抵扣(平台承担=subsidy_cents 口径,审计已兼容);清扫任务兜底补发;三端展示 ETA。
验收:e2e_eta_compensation.py——超时单自动发券且只发一次;券下单抵扣走 subsidy 口径审计绿;天气/改址豁免;未超时不发。
```

### 35. 恶劣天气停运开关

```
在 super-z 仓库开发「极端天气全城停运」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:weather_surcharge(加价,全归骑手)与 night_curfew(深夜保护窗)已有,照后者做即可;缺"极端天气临时停运"。
业务规则(建议拍板):
- platform_flags 加 weather_shutdown(on/off,默认 off):开启即全平台停止接新单,下单 409 文案说明极端天气+已有订单尽力履约;
- 开启同时:抢单大厅顶部横幅提示骑手注意安全、无人接单兜底的自动取消线缩短到 15 分钟(别让用户干等)、商家端横幅;
- 关闭恢复;开关变更推送在线骑手(安全提示);与加价开关互斥性不做强制(可同时开,停运优先生效)。
技术要点:_KNOWN_FLAGS 加 key;create_order 校验(参照 night_curfew);auto_flow 取消线读开关动态缩短;admin.html 平台开关区加一行;三端横幅(用现有公告/flags 下发通道,查 platform.py 是否已有 flags 公开接口,没有就加只读接口)。
验收:e2e_weather_shutdown.py——开启下单 409、兜底提前取消(backdate);关闭恢复;非 admin 改开关 403。
```

### 36. ⚖️ 商家自配送

```
在 super-z 仓库开发「商家自配送选项」。先读 docs/DEV-PROMPTS-2.md 通用约定。

⚖️ 待拍板:配送费归属。建议:自配送单配送费归商家(商家出运力),平台照常只抽餐费 5%,
配送费不进骑手账本(口径同自取单无骑手行)。确认后开工。

现状:所有配送单进骑手抢单池;自取单已有"无骑手行"的完整口径先例(结算/审计/witness 全兼容)。
业务规则(建议草案):
- 商家可开启自配送(店铺设置,可按单切换默认);自配送单不进抢单池、无骑手,商家端多一个"开始配送/已送达"操作流(READY→PICKED_UP→DELIVERED 由商家操作);
- 配送费仍按平台规则计算向用户收取,结算归商家(merchant_earnings 单独行 kind=earning note=自配送费,net=配送费,commission=0——账本恒等式 net==food-commission 要为这类行开新口径,或并入 food 口径:拍板建议独立行);
- 用户端明示"商家自送";骑手侧完全无感;隐私号照常绑定用户↔商家。
技术要点:merchants.self_delivery + orders.self_delivery(下单快照,迁移);抢单池/兜底/改派全部排除;transition 允许 merchant 走配送三态;结算与审计口径(重点:witness 商家行校验 net==food-fee,自配送行 food=配送费 fee=0 可过,注意佣金上限校验分母);e2e 全链路。
验收:e2e_self_delivery.py——自配送单不进池、商家操作到完成、配送费入商家账、佣金只抽餐费;审计+witness 回归绿。
```

---

## 互动与内容线

### 37. ⚖️ 内容审核

```
在 super-z 仓库开发「UGC 内容审核」。先读 docs/DEV-PROMPTS-2.md 通用约定。

⚖️ 待拍板:先发后审(体验好,违规回收)vs 先审后发(稳,但三方未接前人工审不过来)。
建议:文本本地敏感词同步拦截+图片先发后审(三方桩,未配置=只登记待人工抽查)。确认后开工。

现状:评价/菜品名/公告/昵称/图片均无审核;上架应用商店与 EDI 合规都需要审核机制。
业务规则(建议草案):
- 文本(评价/公告/昵称/菜名/工单):本地敏感词库同步校验(政治/色情/辱骂/广告导流),命中 422 提示修改;词库 JSON 入库维护,管理后台可加词;
- 图片(评价图/菜品图/头像/举证图):照支付桩模式接三方(阿里云内容安全 config green_* 字段),未配置=落审核队列表人工看(管理后台「内容审核」面板,驳回则隐藏图+通知);配置后机审,疑似人工复核;
- 审核动作留痕;被驳回内容可申诉走客服工单。
技术要点:services/moderation.py(check_text 同步 + submit_image 异步桩);content_reviews 表(kind, ref_id, url/text, status, note,迁移);各写入口挂钩(评价/公告/菜品/头像/昵称五处);admin 面板;敏感词 Aho-Corasick 或朴素多串匹配(词库几百条够用)。
验收:e2e_moderation.py——敏感词评价 422;图片进队列、驳回后公开列表不展示;白样本通过;非 admin 403。
```

### 38. 订单内 IM 聊天

```
在 super-z 仓库开发「订单内聊天(用户↔骑手 / 用户↔商家)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:WS 基建已有(ws.py manager,订单频道 order:{no} 推状态);沟通靠电话(隐私号)——"放门口""到了打电话"这类事没必要打电话。
业务规则(建议拍板):
- 会话按订单+对象(rider/merchant)建,订单支付后开启、终结(完成/取消)2 小时后只读、7 天后不可见(留档供仲裁,不给用户永久翻);
- 消息类型:文本(过 37 敏感词)、图片、预设快捷语(放门口/到了/请尽快等,一键发);
- 在线走 WS 即达,离线走 JPush(record_skip 不用,高频);未读数角标;
- 电话仍是兜底(隐私号按钮保留);客服/仲裁可调取聊天记录(管理后台按订单查)。
技术要点:messages 表(order_id, sender_id, receiver_role, kind, content, created_at,迁移,索引 order_id);REST 发送+拉历史(轮询兜底)+WS 频道 chat:{order_no} 实时;三端聊天页(样式从简:气泡+输入框+快捷语 chips);未读数存 Redis;order_out 加 unread 计数或独立接口。
验收:e2e_chat.py——双方收发落库;终结 2 小时后发送 409;敏感词拦截;非订单当事人 403;admin 可查。
```

### 39. 订单/店铺分享

```
在 super-z 仓库开发「分享:店铺卡片与订单晒单」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:user_app 已依赖 share_plus(收款账户页用过);无业务分享点;H5 版未建(分享落地页先用官网店铺跳转)。
业务规则(建议拍板):
- 店铺分享:店铺页分享按钮→生成分享图(店名/评分/招牌菜/三原则一句话/下载页二维码)走系统分享;
- 晒单分享:订单完成页/详情页"晒一晒"→分享图带"钱去哪了"三方分账条(全网独一份的传播点)+店名菜品,金额默认打码可开关;
- 落地承接:二维码指向 chaojizan.cc/download(H5 版上线后换成店铺 H5 页,预留 utm 参数统计来源);
- 分享事件埋点(AppEvent 已有体系)统计转化。
技术要点:分享图客户端 Canvas 绘制(shared 出 ShareCardPainter,复用品牌色/logo,不依赖服务端渲染);二维码用纯 Dart 包(qr 系列,依赖要三端加);share_plus 调系统分享;埋点 track('share_shop'/'share_order')。
验收:flutter analyze 零问题+Web 端手测分享图生成正确(自动化只测埋点接口);分享图含打码金额开关。
```

### 40. ⚖️ 拼单

```
在 super-z 仓库开发「拼单(共享购物车)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

⚖️ 待拍板:结算模式。建议最简:发起人开车→同伴实时加菜→发起人一次性支付(AA 线下自行解决,
平台不碰代收分账——资金合规红线)。确认后开工。范围也可拍板砍成"仅同 WiFi 面对面拼单"。

现状:购物车是客户端本地状态;无多人协作概念;H5 未建(同伴无 App 则无法参与,首版可要求都装 App)。
业务规则(建议草案):
- 发起人在店铺页开拼单→生成 6 位拼单码(2 小时有效);同伴 App 内输码进入同一购物车(同店);
- 实时同步:各自增删自己的菜(标记谁点的),WS 广播;发起人可锁单→去结算(锁后不可改),支付即成单,备注自动带"拼单×N人";
- 订单归发起人(积分/售后/评价都是发起人的,首版不拆);同伴看到"已下单"终态;
- 起送价/满减按合车总额算(天然优势场景)。
技术要点:拼单态存 Redis(cart:{code} JSON+TTL,不建表——未成单的车不落库);WS 频道 cart:{code};REST:开车/入车/增删/锁定/查车;下单接口收 group_code 校验并原子关车;用户端拼单页(成员列表+分组菜品)。
验收:e2e_group_cart.py——两账号同车增删同步;锁后加菜 409;下单关车;过期码 404;满减按合计算。
```

---

## 运营增长线

### 41. 多城市/商圈隔离

```
在 super-z 仓库开发「多城市运营隔离」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:用户端按定位半径找店(PostGIS),天然"看得见的才点得到";但运营视角没有城市概念——后台列表全国混排、统计无法分城、开新城无法灰度。
业务规则(建议拍板):
- merchants 加 city(申请入驻时从坐标逆地理解析,天地图服务端 key 已有,失败人工填;管理后台可改);
- 骑手加 city(实名审核时定,只看/只抢本城订单——跨城抢单没有意义还费流量);
- 管理后台全局城市筛选器(商家/订单/提现/看板/运力全套带 city 过滤);数据看板分城 tab;
- 开城管理:platform_flags 加 open_cities 清单,不在清单的城市商家可入驻待审但不可营业(文案:你的城市即将开通),抢先注册留资。
技术要点:两表加 city 迁移(存量回填:逆地理批量跑一次,脚本);services/geo_city.py(天地图逆地理封装,TIANDITU_SERVER_KEY 进 config);列表接口加 city 参数(admin 全套);骑手池过滤;开城 flag 校验挂 merchants 开店处。
验收:e2e_multi_city.py——A 城骑手看不到 B 城单;后台按城筛;未开城商家开店 409;逆地理失败降级人工。
```

### 42. 商家经营分析(含菜品销量排行)

```
在 super-z 仓库开发「商家经营分析页」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:商家端有对账(按日账单)、质量(超时率/拒单)、阶梯佣金卡;没有经营视角——什么时段卖得好、什么菜卖得好、客单价、复购,商家全靠感觉。
业务规则(建议拍板):
- 商家端新「经营分析」页(对账页入口):近 7/30 天切换——①时段分布(24 小时下单柱状) ②菜品销量排行 TOP10(份数/金额,含估清与售罄损失提示"售罄时段错过约 N 单") ③客单价趋势 ④复购率(30 天内下过 ≥2 单的用户占比) ⑤配送/自取占比;
- 全部只读统计,不做建议不做排名对比(不制造商家焦虑);数据口径与对账一致(完成单)。
技术要点:GET /merchants/me/analytics?days=7|30 一个接口聚合返回(SQL 聚合,orders.items JSONB 展开统计菜品——用 jsonb_array_elements,注意排除 0 元赠品行);图表用商家端现有简易绘制(对账页已有柱状先例,没有就 CustomPaint 画简单柱状,不引 charts 库);售罄损失=估清时段的历史同时段均量估算(粗口径,标注"估算")。
验收:e2e_merchant_analytics.py——造多单后各维度数字与手算一致;赠品行不计销量金额;非本店 403。
```

### 43. 高峰备货建议

```
在 super-z 仓库开发「高峰备货提示」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:库存有每日回满(#18)与估清(#19);商家备多少全靠经验;42 提供了时段×菜品数据基础。
业务规则(建议拍板):
- 每天上午 10:00 与下午 16:00(饭点前)给商家推送备货提示:按近 14 天同餐段(午市 10-14/晚市 16-21)各菜品销量 P80 给建议份数,与当前库存对比,列"可能不够卖"的菜 TOP5;
- 商家端库存页顶部备货卡(同口径,随时可看);一键按建议补库存(批量改 stock,可单菜调整);
- 纯建议不自动改库存;数据少于 7 天的菜不建议(标"数据积累中")。
技术要点:services/stocking.py(P80 分位统计,SQL 聚合复用 42 的展开);auto_flow 挂两个提醒窗口(参照 04:00 每日任务,Redis 防重按日+餐段);推送 record_skip;POST /merchants/me/dishes/batch-stock(批量补库存,复用估清解除逻辑);商家端备货卡+一键采纳。
验收:e2e_stocking.py——造 14 天历史后建议值=P80;库存充足的菜不出现;一键采纳后库存生效且解除估清;提醒防重。
```

### 44. 防刷单风控

```
在 super-z 仓库开发「刷单与补贴滥用风控(标记不拦截)」。先读 docs/DEV-PROMPTS-2.md 通用约定。

现状:限流有(登录/验证码/下单频次);无业务风控——首单立减可被小号薅、商家可自刷 GMV 冲销量排名、同地址高频下单无感知。
业务规则(建议拍板):
- 规则引擎轻量版,先做四条:①同收货地址(坐标 50m 内)24h 内 ≥N 单且多账号 ②新注册账号 1h 内下单且用首单立减(标记,补贴照给——宁可错给不可错杀) ③商家老板/员工账号给自己店下单(手机号关联+管理员标注的关联账号) ④同设备多账号(客户端上报 device_id,轻量指纹,不引 SDK);
- 命中只做三件事:订单打 risk_flags 标记(JSONB)、管理后台「风控」面板可查、月售/销量排行等运营指标排除标记单(资金结算照常——钱是真付的);
- 人工复核可解除/确认标记;确认刷单的商家累计 3 次管理后台标黄提示约谈(不自动处罚)。
技术要点:orders.risk_flags JSONB+users.device_id(迁移);services/risk.py(下单后异步评估,不阻塞下单主流程);月售统计/排行 SQL 排除 confirmed 标记;admin 面板;客户端登录时上报 device_id(package_info+android id 拼接哈希)。
验收:e2e_risk.py——同址高频/新号补贴/同设备触发标记;结算不受影响;月售排除确认单;解除标记恢复计数。
```

---

## 建议开发顺序

- **合规优先**(上架与真实运营的前置):37 内容审核 → 44 防刷单 → 35 停运开关
- **运力线**:25 小费 → 26 改派 → 27 运力看板 → 28 半径 → 29 考勤 → 30 上岗 → 31 保险 → 32 事故 →(拍板后)33 转单考核 / 34 超时赔付
- **增长线**:41 多城市 → 42 经营分析 → 43 备货 →(H5 之后)39 分享 → 40 拼单
- **互动**:38 IM(依赖 37 的敏感词)
- ⚖️ 待拍板五处:33 拒单→转单考核改写、34 赔付口径、36 自配送费归属、37 先发后审、40 拼单结算模式

依赖:38 依赖 37(敏感词);39/40 的传播闭环依赖 H5 版(第一辑遗留,建议单独立项先做);31↔32 联动;42 是 43 的数据基础;27 用 41 的城市维度更好(不强依赖)。
```
