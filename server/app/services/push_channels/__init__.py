"""推送通道:一条通道一个模块,上面由 services/push.py 按设备选路(#384)。

已经接好的:
- `apns` —— 苹果。直连,免费无配额,认证是一个 .p8 密钥。

还没接的(按用户 2026-09-16 定的顺序,下一批):
- 华为 HMS、小米、OPPO、vivo —— 国内安卓上 App 被杀掉之后,
  **只有厂商自己的通道叫得醒它**,各家免费但要各自注册、各自过审。

一条都没配的时候不是"静默跳过":push_logs 里照样记一条,写明是哪条通道没接 ——
"推送没到"这件事没人会来报 bug,只能靠日志自己看得见。
"""
