"""更正第 1 纪元的公开说明:那句「schema 2 起演示流水不再进账本」是错的

0110 写下这段说明时,采用的还是「把演示流水从账本里过滤掉」的方案。
那个方案当天就被推翻了(e0a8e6f):按手机号段静默省略,等于给
「公开账本包含当天全部流水」加一条看不见的例外,而看不见的例外
正是这套东西要消灭的。SCHEMA 也回到了 1。

于是生产上这条纪元记录里留着一句与事实不符的话 —— 而它是要**长期挂在
/nodes 与透明中心**给外人看的。纪元记录的全部意义就是"平台说过什么算数",
里面留一句错的,比没有这条记录更糟。

## 为什么是改而不是再起一条

纪元记录一旦被见证节点留存就不该再动 —— 动了就是篡改。但这一条:

- 2026-08-25 13:30 才写进生产,离现在几分钟;
- 目前没有任何见证节点认识 /ledger/epochs(0.1.3 还没发出去),
  也就不可能有人留存过它。

所以现在改是安全的,而且是唯一一次机会。再往后就只能追加一条更正,
不能改原文。

同时把 0110 的文案也改对,免得新装的城市从一开始就带着这句错话。
"""
from alembic import op

revision = '0111'
down_revision = '0110'
branch_labels = None
depends_on = None

_RIGHT = (
    '2026-07-28 清理生产环境的演示数据(scripts/scrub_demo.py)。'
    '当时脚本按设计清空了 ledger_anchors 让链重新起链,此前的每日锚点'
    '因此消失;链尾哈希当时没有保留,这一栏是空的。'
    '后来查明:清账本本就没有必要 —— 锚点存的是当天流水的全文快照,'
    '底层单据删掉之后照样自洽,复算得出同样的哈希。脚本已改为不动账本,'
    '真要重置必须先冻结旧链链尾并留下这样一条记录。'
)


def upgrade() -> None:
    op.execute(
        "UPDATE ledger_epochs SET reason = '%s' WHERE epoch = 1"
        % _RIGHT.replace("'", "''"))


def downgrade() -> None:
    # 不还原成那句错的
    pass
