# 超级赞用户端 · 鸿蒙原生版(ArkTS)

鸿蒙 NEXT 之后**彻底移除了 AOSP 兼容层,APK 装不上去**,所以这不是"适配",
是一个独立的原生应用。骑手端和商家端暂不做鸿蒙(理由见
[docs/DEV-PROMPTS-25.md](../../docs/DEV-PROMPTS-25.md))。

## 状态

**这一版是盲写的,没有经过编译。** 本仓库的开发机上没有 DevEco Studio
和鸿蒙 SDK,`.hap` 构建和 CI 都跑不了。代码按华为官方文档(Stage 模型、
Network Kit、Location Kit、ArkData)写成,**第一次在 DevEco 里打开必然要修
一批类型和 API 签名** —— 这是选择盲写的必然代价,不是质量问题。

**主链路已闭环**:隐私门 → 短信登录 → 首页(定位 + 附近商家)→ 店铺菜单 →
购物车 → 结算(地址/选券/备注/自取)→ 下单 → 支付 → 订单列表 → 订单详情
(含「这一单的钱去哪了」)。共 2889 行 ArkTS。

本机能做的检查都做了:96 个跨文件导入全部解析、无未使用导入、
10 个 json/json5 配置全部可解析。**但这些不等于能编译** ——
ArkTS 的类型检查、装饰器规则、UI 语法都要 DevEco 才验得了。

未完成:微信支付 SDK(缺 HAR 包与 appId)、地址簿增删改、推送、
搜索与筛选、评价、售后、团购券、住宿、跑腿、透明中心原生页
(现在跳网页版)。

## 怎么跑起来

1. 装 **DevEco Studio 6**(HarmonyOS SDK API 20);
2. `File > Open` 选中 **本目录**(`apps/user_app_harmony`),不要选仓库根目录
   —— 根目录是 Flutter/Python 的工程,DevEco 认不出来;
3. 等 `hvigor` 同步完依赖;
4. `File > Project Structure > Signing Configs` 里配一次调试签名
   (需要登录华为开发者账号,自动签名即可);
5. 起模拟器或连真机,Run。

后端地址在 `entry/build-profile.json5` 的 `buildProfileFields.SUPERZ_API`,
默认指向线上。要连本机后端就改成 `http://<你的内网 IP>:8010` ——
注意**不能用 `127.0.0.1`**,那在模拟器里指的是模拟器自己。

## 几条定死的规矩

这些在 Flutter 端是踩过坑才立的,鸿蒙版直接照做,别再走一遍:

- **网络只有一个出口**(`common/Api.ets`)。任何页面都不许自己
  `http.createHttp()` —— 一旦开口子,超时、鉴权、401、错误文案就会各写一套;
- **隐私门在最外层**,同意之前一个请求都不许发。安卓端为此返过一次工;
- **金额全程用「分」的整数**,客户端不做浮点运算;
- **中文名用服务端下发的**(`status_label` / `dine_in_label` /
  `fee_part_labels`),不在客户端另写映射;
- **定位拿不到要明说**,不要静默换成演示城市坐标让用户以为看的是身边的店;
- **明厨亮灶和堂食标识两种都要标**,没有的店显示"无"/"未填报",
  这是总局令第 123 号的法定公示项,不是加分徽章。

## 上架

华为激励 2026:新应用 3000 元/款,报名截止 9-25、上架截止 9-30,
先到先得发完即止。

**卡点是纸不是代码**:App 备案(工信部)和软著都还没办,而华为应用市场
审核要资质,软著正常流程一到两个月。这两件事的优先级高于写代码 ——
代码写完证没下来,一样上不了架。
