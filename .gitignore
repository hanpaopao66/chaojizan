# Python
__pycache__/
*.pyc
.venv/
server/.env
server/uploads/
# 微信支付商户私钥/证书(绝不入库)
server/certs/

# Flutter / Dart
.dart_tool/
.packages
build/
# Kotlin 编译器守护进程的会话文件,每次 gradle 构建都会新生成
apps/*/android/.kotlin/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
# ⚠️ 平台目录**全部入库**,不要再加回 gitignore。
#
# 原本只留了 android/ 和 ios/,其余四个由 flutter create 再生 ——
# 那在只发安卓 iOS 的时候没问题,一旦要发 web 和桌面就不行了:
# 这些目录装着**必须定制、丢了就出事**的东西。
#
# - web/index.html 的 base href、web/manifest.json 的 PWA 名称与图标
#   (不改的话标题就叫 "user_app",装到桌面也是这个名);
# - macos/Runner/*.entitlements 的 com.apple.security.network.client ——
#   **少这一条 macOS 版一个网络请求都发不出去**,而它是 flutter create
#   的默认模板,再生一次就回去了;
# - windows/runner 的应用名、图标、版本资源。
#
# 和 android/ ios/ 已入库是同一个道理:凡是需要手工定制的,就得入库。
#
# 构建产物仍然不入库(下面的 build/ 挡着)。
# ⚠️ 这里**不要**再加 apps/*/test/。
#
# 曾经加过,本意大概是挡掉 flutter create 生成的 widget_test.dart 模板,
# 结果连带把真写的测试也挡在仓库外 —— 商家端的小票版式测试在本机跑了
# 很久、还被当成"版式已验证"的依据,而它从来没进过仓库:别人 clone
# 下来没有,CI 也跑不了。
#
# 模板测试该做的是删掉,不是靠 gitignore 藏起来。

# CocoaPods 的依赖产物:`pod install` 再生,几百 MB,不入库。
# **Podfile 和 Podfile.lock 要入库** —— 前者是配置(我们改过:
# 写死 platform :ios 13.0、use_frameworks! 带 :linkage => :static,
# 后者是腾讯地图静态库的硬要求),后者锁版本保证各机器装出同一套。
apps/*/ios/Pods/
apps/*/ios/.symlinks/
apps/*/macos/Pods/
apps/*/macos/.symlinks/

# IDE
.idea/
.vscode/
.DS_Store
dist/
/index.html
server/appdist/

# 官网(web/)
web/node_modules/

# 内部运营策略(不公开)
marketing/
server/app_version.txt
# 部署机地址等本机部署参数(不入库)
deploy/.env.deploy
# 生产密钥与证书:只应存在于部署机(样例是 .env.prod.example / frpc.toml.example)。
# 这些路径此前不在 ignore 里 —— 照开城手册在本地准备文件的人一不小心
# 就会把真密钥提交进公开仓库,宁可多挡一层
deploy/.env.prod
deploy/tunnel/frpc.toml
deploy/certs/
deploy/letsencrypt/
deploy/certbot-www/
deploy/wxpay-certs/
# 内部路线图(公开仓不发布)
docs/ROADMAP.md
merchant-web/node_modules/
admin-web/node_modules/
